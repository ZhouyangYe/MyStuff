import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'cana-profile',
    template: '<router-outlet></router-outlet>'
})

export class ProfileComponent{
    constructor(){
    }
}