import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'cana-admin',
    template: '<router-outlet></router-outlet>'
})

export class AdminComponent{
    constructor(){
    }
}